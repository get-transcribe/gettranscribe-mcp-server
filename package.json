{
  "name": "gettranscribe-mcp-lambda",
  "version": "1.0.0",
  "description": "AWS Lambda MCP server for GetTranscribe",
  "type": "module",
  "main": "lambda-handler.js",
  "scripts": {
    "build": "zip -r gettranscribe-mcp-lambda.zip lambda-handler.js package.json node_modules/",
    "deploy": "aws lambda create-function --function-name gettranscribe-mcp --runtime nodejs18.x --role arn:aws:iam::YOUR_ACCOUNT:role/lambda-execution-role --handler lambda-handler.handler --zip-file fileb://gettranscribe-mcp-lambda.zip",
    "update": "aws lambda update-function-code --function-name gettranscribe-mcp --zip-file fileb://gettranscribe-mcp-lambda.zip"
  },
  "dependencies": {
    "@aws-lambda-powertools/commons": "^2.27.0",
    "axios": "^1.7.7"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": [
    "aws",
    "lambda",
    "mcp",
    "serverless",
    "transcription",
    "sse"
  ]
}
